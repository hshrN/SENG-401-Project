# Database connection (use this if PostgreSQL requires a password)
# DATABASE_URL=postgresql://USERNAME:PASSWORD@localhost/401GameDB
# Example: postgresql://carlsoriano:mypass@localhost/401GameDB
# If no auth needed: postgresql://localhost/401GameDB
DATABASE_URL=postgresql://localhost/401GameDB

# Flask configuration
FLASK_ENV=development
FLASK_DEBUG=1
