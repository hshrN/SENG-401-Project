[build]
builder = "nixpacks"

[deploy]
startCommand = "flask db upgrade && gunicorn app:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
