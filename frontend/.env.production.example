# Copy to .env.production and fill in the real Railway backend URL before building.
# Do not commit .env.production if it contains real secrets.
REACT_APP_API_URL=https://your-railway-domain.up.railway.app
